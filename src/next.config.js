/** @type {import('next').NextConfig} */
module.exports = {
    reactStrictMode: true,
    turbopack: {
        root: __dirname,
    },
    typescript: {
        // ignoreBuildErrors: false,
    },
};
